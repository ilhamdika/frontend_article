import React, { useEffect, useState } from "react";
import { DotLoader } from "react-spinners";

export const AddNew = () => {
  return <h1 className="text-3xl font-bold underline">Add New</h1>;
};

export default AddNew;
